/**
 * A library for building mocypress-ui-core dependency lib
 *
 * @remarks
 * The mocypress-ui-core defines the {@link Method} enum class,
 * which are used to define and store type of the locators
 *
 * @packageDocumentation
 */

/**
 * Enum class define and store all type of access locators type
 * Xpath = If element capture using xpath
 * Cypress_get = If element capture other than xpath, or any type of locators( id, css, class, etc..)
 * @public
 */
export enum Method{
    XPATH,
    CYPRESS_GET
}
