import {BaseTextField} from "../base-elements/BaseTextField";
import {Method} from "../generalize/Method";
import {BaseCheckBox} from "../base-elements/BaseCheckBox";
import {BaseDropDown} from "../base-elements/BaseDropDown";

/**
 * A library for building mocypress-ui-core dependency lib
 *
 * @remarks
 * The mocypress-ui-core defines the drop-down component connect with {@link BaseDropDown} and {@link Element} class,
 * which are used to build generic drop-down
 *
 * @packageDocumentation
 */

/**
 * Class expose drop-down component using its behaviours and actions to test level.
 * @public
 */
export class DropDown extends BaseDropDown{

    constructor(locator: string, method: Method = Method.XPATH) {
        super(locator, method);
    }
}